<Shell
    x:Class="Cinestar_app.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:Cinestar_app.Pages"
    Title="Cinestar_app">

    <TabBar>
        <ShellContent Title="Home"  ContentTemplate="{DataTemplate pages:MainPage}" />
        <ShellContent Title="Movies"  ContentTemplate="{DataTemplate pages:Filmovi}" />
        <ShellContent Title="Loyalty"  ContentTemplate="{DataTemplate pages:LoyaltyBodovi}" />
        <ShellContent Title="Profile"  ContentTemplate="{DataTemplate pages:Profil}" />
    </TabBar>


</Shell>
